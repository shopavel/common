<?php

return array(

    /*
    |--------------------------------------------------------------------------
    | Table Name Prefix
    |--------------------------------------------------------------------------
    |
    | To prevent conflicts with other packages or your own code you can prefix
    | the shopavel table names with a custom value. By default this is empty
    | so tables will have no prefix.
    |
    */

    'prefix' => '',

);